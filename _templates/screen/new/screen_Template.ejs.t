---
to: "frontend/src/screens/<%= name %>/index.js"
---

import React, { useState, useEffect } from 'react';
import PropTypes from 'prop-types';
import './styles.css';

const <%= (name) %> = () => {};

  return (
    <div className='<%= (name) %>'>
      <%= (name) %>
    </div>
  );


<%= (name) %>.propTypes = {};

<%= (name) %>.defaultProps = {};

export default <%= (name) %>;



